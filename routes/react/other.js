import Other from 'components/pages/other.jsx';

const route = {
        path: '/other',
        exact: false,
        component: Other,
        loadData: (ctx, context, Store)=>{
            return
        }
}

export default route
